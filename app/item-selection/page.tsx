// This page will be used when a user selects an item on the home page to see more information. I have only stubbed this out.
// Eventually this will deal with handling a selection from the homepage and pulling all relevant information from db or classes 

export default function ItemSelection() {
  return (
    <main
      data-theme="forest"
      className="flex min-h-[calc(100vh-4rem)] w-full flex-col items-center justify-center bg-base-100 py-2 text-base-content"
    >
      <h1 className="text-4xl font-bold mb-4">Welcome to Item Selection!</h1>
    </main>
  );
}